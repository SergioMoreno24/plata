import React from 'react';

const Title = ({ title }) => {
  return (
    <div className='w-full border-b-4 border-blue-exm3 text-center font-black text-lg text-blue-exm3'>{ title }</div>
  )
}

export default Title;